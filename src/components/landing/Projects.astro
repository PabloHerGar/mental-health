---
import { projects } from '../../config/projects.json'
---

<!-- <section> <- No hace falta un section para cada componente si no va a haber partes diferenciadas entre sí en la misma página
  <h2>Proyectos realizados</h2>
  {
    projects.map(project => (
      <article class='articleproject '> <- Aquí sí que tienen sentido las sections, para separar los proyectos entre sí
        <h3>{project.title}</h3>
        <p>{project.description}</p>

        <a class='btnbody' href={project.url} target='_blank'>
          + info
        </a>

        <img src={project.image} class='imgproject' alt='' />
      </article>
    ))
  }
</section> -->
<section>
  <h2>Proyectos realizados</h2>
  {
    projects.map(project => (
      <article class='article_projects '>
        <h3>{project.title}</h3>
        <p>{project.description}</p>

        <a class='btn_body' href={project.url} target='_blank'>
          + info
        </a>

        <img src={project.image} class='img_projects' alt='' />
      </article>
    ))
  }
</section>
