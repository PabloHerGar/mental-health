---
// import * as features from '../config/features.json' <- Una importación de todos los elementos del archivo JSON elimina el error de importación por defecto. Para una importación más sencilla, mirar Projects.astro.
import features from '../../config/features.json'

// Una interfaz es una forma de definir un tipo de dato en Typescript.
/* 
  interface Feature {
    title: string
    description: string
  }
*/
---

<section>
  <h2>Habilidades</h2>

  <!-- La etiqueta <ol> se utiliza para enumerar elementos. Para listas que no requieran ordenación es mejor utilizar <ul> -->
  <ol class='article_features'>
    {
      features.map(
        (
          feature // (feature: Feature) <- El tipo de dato del parámetro se debe definir, en este caso arriba como interfaz.
        ) => (
          <li class='li_features'>
            <h3>{feature.title}</h3>
            <p>{feature.description}</p>
          </li>
        )
      )
    }
  </ol>
</section>
